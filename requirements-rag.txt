# RAG (embeddings + FAISS) and OCR stack.
# Heavier dependencies (pulls in torch). Used by the Docker image and by
# local installs that want embedding-based retrieval / image OCR.
# On very new Python versions where torch has no wheels yet, skip this file
# and run with RETRIEVAL_MODE=full and OCR_ENABLED=false (see README).
-r requirements.txt

sentence-transformers>=3.0
faiss-cpu>=1.8
easyocr>=1.7
pillow>=10.0
