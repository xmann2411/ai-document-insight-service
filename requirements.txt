# Core runtime - lightweight, installs on any modern Python (incl. 3.13/3.14).
# The RAG + OCR stack lives in requirements-rag.txt (used by the Docker image).
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.35
python-multipart>=0.0.9
pymupdf>=1.24
anthropic>=0.40
python-dotenv>=1.0
pydantic-settings>=2.4
numpy>=1.26
